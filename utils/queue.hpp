#pragma once

#ifndef MYLK_UTILS_QUEUE_HPP
#define MYLK_UTILS_QUEUE_HPP

/*
	Queue.
	A wrapper over vector which restricts it to a queue interface.
*/

namespace mylk {
	struct Queue {

	};
}

#endif
